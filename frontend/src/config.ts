// ── Platform Configuration ─────────────────────────────────────────────────
// Centralized settings — change currency here, it applies everywhere.

// Currency symbol displayed in all monetary values.
// Change to 'VND', 'USD', '$', etc. as required for each client.
export const CURRENCY_SYMBOL = '₫';

// Locale for number formatting (e.g. 'en-MY', 'vi-VN', 'en-US')
export const CURRENCY_LOCALE = 'vi-VN';

// Platform name shown in the UI
export const PLATFORM_NAME = 'Vietnam Claims Platform';
export const PLATFORM_TAGLINE = 'Central Code Master Demonstration';

// Note: This is a prototype. Benchmark and benchmark values are representative demo data.
export const IS_PROTOTYPE = true;
